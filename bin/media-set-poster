#!/bin/bash

set -e

source $(which media)
summary_line "$*" 'add poster image(s) to a file'


file="$1"
shift

for poster in "$@"; do
    AtomicParsley           \
        "$file"             \
        --overWrite         \
        --artwork "$poster"
done
