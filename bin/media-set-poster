#!/bin/bash

set -e

# shellcheck source=/dev/null
source $(which media)
summary_line "$*" 'add poster image(s) to a file'


file="$1"
shift

for poster in "$@"; do
    AtomicParsley           \
        "$file"             \
        --overWrite         \
        --artwork "$poster"
done
